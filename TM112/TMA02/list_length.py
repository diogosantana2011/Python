# Problem: Compute length of list
items = [5, -8, 3, 1, 90]

# Output: Length of list, i.e. how many items it has
length = 0
for item in items:
    length = length + 1

print('The Length of list: ', items, 'is: ', length)